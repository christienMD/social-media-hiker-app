const Navbar = ({ className }: { className: string }) => {
  return (
    <nav
      className={`h-16 w-full bg-social-bg flex items-center px-2 ${className}`}
    >
      Navbar
    </nav>
  );
};

export default Navbar;
